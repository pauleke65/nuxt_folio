[build]
  command = "yarn generate"
  # ssr: false uses static preset → outputs to dist/ (not .output/public)
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
